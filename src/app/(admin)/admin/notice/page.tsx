"use client";

import { DesktopTable } from "@/component/notice/UI/dashboard/DesktopTable";

const Page = () => {
  return (
    <div>
      <DesktopTable data={[]} onItemClick={() => {}} />
    </div>
  );
};

export default Page;

import { color } from "@/component/shared/designed/color";
import { style } from "@vanilla-extract/css";

export const noticePageContainer = style({});

export const noticeContainer = style({
  maxWidth: "1520px",
  margin: "auto"
});
